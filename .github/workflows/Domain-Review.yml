name: Domain Review Notification

on:
  pull_request:
    types: [opened]

jobs:
  notify-teams:
    runs-on: ubuntu-latest

    steps:
      - name: Send Domain Review Notification
        run: |
          curl -H "Content-Type: application/json" \
          -d '{
            "type":"message",
            "attachments":[
              {
                "contentType":"application/vnd.microsoft.card.adaptive",
                "content":{
                  "$schema":"http://adaptivecards.io/schemas/adaptive-card.json",
                  "type":"AdaptiveCard",
                  "version":"1.4",
                  "body":[
                    {
                      "type":"TextBlock",
                      "size":"Large",
                      "weight":"Bolder",
                      "text":"📢 Domain Review Required"
                    },
                    {
                      "type":"TextBlock",
                      "text":"Repository: ${{ github.repository }}"
                    },
                    {
                      "type":"TextBlock",
                      "text":"Author: ${{ github.actor }}"
                    },
                    {
                      "type":"TextBlock",
                      "text":"PR Title: ${{ github.event.pull_request.title }}"
                    },
                    {
                      "type":"TextBlock",
                      "wrap": true,
                      "text":"A new Pull Request requires domain review before it can proceed."
                    }
                  ],
                  "actions":[
                    {
                      "type":"Action.OpenUrl",
                      "title":"Review PR",
                      "url":"${{ github.event.pull_request.html_url }}"
                    }
                  ]
                }
              }
            ]
          }' \
          "${{ secrets.TEAMS_WEBHOOK_URL }}"
